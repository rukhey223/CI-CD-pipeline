steps:
# Step 1: Install dependencies and run tests
  - name: 'python:3.12-slim'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        pip install --upgrade pip
        pip install -r requirements.txt
        pytest --maxfail=1 --disable-warnings -q

# Step 2: Build Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args:
     - 'build'
     - '-t'
     - 'us-central1-docker.pkg.dev/$PROJECT_ID/my-repo/app:$SHORT_SHA'
     - '.'

# Step 3: Push image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
     - 'push'
     - 'us-central1-docker.pkg.dev/$PROJECT_ID/my-repo/app:$SHORT_SHA'

# Step 4: Update manifest in environment repo
  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
    - '-c'
    - |
      - |
        # Use the token directly without brackets
        git clone https://rukhey223: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@github.com/rukhey223/gitops-env.git
        cd gitops-env
        # This matches the 'kubernetes.yaml' file I see in your screenshot
        sed -i "s|image: .*|image: us-central1-docker.pkg.dev/$PROJECT_ID/my-repo/app:$SHORT_SHA|g" kubernetes.yaml
        git config --global user.email "build-bot@google.com"
        git config --global user.name "Cloud Build Bot"
        git add kubernetes.yaml
        git commit -m "Update image to $SHORT_SHA"
        # Specify 'origin main' to avoid the 'no upstream' error
        git push origin main
options:
 logging: CLOUD_LOGGING_ONLY
