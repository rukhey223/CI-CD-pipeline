apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
spec:
  replicas: 2
  template:
    spec:
      containers:
      - name: app
        image: us-central1-docker.pkg.dev/YOUR_PROJECT_ID/my-repo/app:latest
